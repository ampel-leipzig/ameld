% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arcv.glmnet.R
\name{arcv.glmnet}
\alias{arcv.glmnet}
\alias{which.min.error}
\alias{print.arcv.glmnet}
\title{Elastic Net with Repeated Cross-Validation for cv.glmnet}
\usage{
arcv.glmnet(
    x, y,
    lambda = NULL,
    alpha = seq(0L, 1L, by = 0.1),
    nrepcv = 100L, nfolds = 10L, foldid = NULL, balanced = FALSE,
    ...,
    trace.it = interactive()
)

which.min.error(x, s = c("lambda.1se", "lambda.min"), maxnnzero = Inf)

\method{print}{arcv.glmnet}(x, digits = max(3L, getOption("digits") - 3L), ...)
}
\arguments{
\item{x}{\code{arcv.glmnet} object.}

\item{y}{response as in \code{cv.glmnet}.}

\item{lambda}{\code{numeric}, optional user-supplied lambda sequence; default is
\code{NULL} and \code{glmnet} chooses its own sequence.}

\item{alpha}{\code{numeric}, different \code{alpha} values that should evaluated (0 =
ridge regression, 1 = lasso regression).}

\item{nrepcv}{\code{integer(1)}, number of repeated cross-validations (outer
loop).}

\item{nfolds}{\code{integer}, number of folds, same as in \code{cv.glmnet}.}

\item{foldid}{\code{matrix}, an optional matrix with \code{nrepcv} rows and
\code{nrow(x)} columns containing ids from 1 to \code{nfolds} identifying what fold
each observation is in. If given \code{nrepcv} and \code{nfolds} are ignored.}

\item{balanced}{\code{logical}, should classes/status be balanced in the folds
(default: FALSE)?}

\item{\dots}{further arguments passed to \code{cv.glmnet}.}

\item{trace.it}{\code{integer}, if \code{trace.it = 1}, then a progress bar is
displayed.}

\item{s}{\code{character}/\code{numeric}, value(s) of the penality parameter \code{lambda}.
See \code{\link[glmnet:predict.cv.glmnet]{glmnet::predict.cv.glmnet()}} for details.}

\item{maxnnzero}{\code{numeric(1)}, maximum number of allowed non-zero beta
coefficients. Default is \code{Inf} which selects the model with the minimal error
(the measurement error is chosen from all \code{"lambda.min"} or \code{"lambda.1se"}
results depending on \code{s}). If a number is given the model with the lowest
(local) error that has at the most \code{maxnnzero} non-zero beta coefficents
is chosen (also based on the given \code{s}, as described above). If no model has
less than \code{maxnnzero} coefficients the simplest model is chosen and a warning
given.}

\item{digits}{\code{integer(1)}, number of digits shown in table.}
}
\value{
An object of class \code{arcv.glmnet} that extends the \code{rcv.glmnet} and
\code{cv.glmnet} class.

\code{numeric} index of model with minimal error.
}
\description{
This functions evaluates elastic net repeated cross validation for alpha and
lambda based on \code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}}.
}
\examples{
# Examples taken from ?"glmnet::cv.glmnet"
set.seed(1010)
n <- 1000
p <- 100
nzc <- trunc(p/10)
x <- matrix(rnorm(n * p), n, p)
beta <- rnorm(nzc)
fx <- x[, seq(nzc)] \%*\% beta
eps <- rnorm(n) * 5
y <- drop(fx + eps)
set.seed(1011)
# nrepcv should usually be higher but to keep the runtime of the example low
# we choose 2 here
arcvob <- arcv.glmnet(x, y, alpha = c(0, 0.5, 1), nrepcv = 2, nfolds = 3)
plot(arcvob)
title("Gaussian Family", line = 2.5)
plot(arcvob, what = "lambda.min")
}
\references{
Jerome Friedman, Trevor Hastie, Robert Tibshirani (2010).
Regularization Paths for Generalized Linear Models via Coordinate
Descent. Journal of Statistical Software, 33(1), 1-22. URL
\url{https://www.jstatsoft.org/v33/i01/}.

Noah Simon, Jerome Friedman, Trevor Hastie, Rob Tibshirani (2011).
Regularization Paths for Cox's Proportional Hazards Model via
Coordinate Descent. Journal of Statistical Software, 39(5), 1-13. URL
\url{https://www.jstatsoft.org/v39/i05/}.
}
\seealso{
\code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}}
}
\author{
Sebastian Gibb
}
